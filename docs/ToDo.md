# Todo

- rename picoclaw binary to `picoclaw-binary-mac` and import new picoclaw binary `picoclaw-binary-win` so that on setup auto detect os type -> rename -binary-os to `picoclaw` and load that binary into config

- ensure config.json uses `YOUR_TELEGRAM_BOT_TOKEN`

- test on both win + mac
- clean up all

- update ReadMe.md w/ all steps

- optimize, clean up, reformat, aesthetic ?