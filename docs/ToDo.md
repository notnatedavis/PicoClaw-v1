# Todo

---

- (FIX) :
    - clean should wipe logs/picoclaw.log + delete PicoClaw-v1/picoclaw if exists (backup exists within binaries/ untouched)
    - odd model output responses, tuned out / other

---

- (UPDATE) :
    - scripts/
        - stop.sh ; should clear (workspace/sessions/* && workspace/memory/*) not important files to be kept
        - setup.sh ; should clear (workspace/sessions/* && workspace/memory/*) not important files to be kept
        - healthcheck.sh ; should clear (workspace/sessions/* && workspace/memory/*) not important files to be kept

    - RepoVisGoal
    - ReadMe
    - ToDo
    - All files
        - comments, style, quality, format, etc.

---

- (FUTURE) :
    - skills & agents
        - /docx skill
        - /xlx skill
        - google cal skill
    - workspace/